#include <stdio.h>

/**
 * main - assign the alphabet.
 *
 * Return: 0 indicates sucesfull execution.
 */

int main(void)
{

int a = 48;

while (a <= 57)
{

putchar(a);

a = a + 1;

putchar(',');
putchar(' ')

}
putchar('\n');
return (0);
}
