#include <stdio.h>

/**
 * main - assign the alphabet.
 *
 * Return: 0 indicates sucesfull execution.
 */

int main(void)
{

int a = 0;

while (a <= 9)
{

printf("%d", a);

a = a + 1;

}
printf('\n');
return (0);
}
