#include <stdio.h>
/**
 * main - use printf, no allow use puts
 * Description: uses printf to print message and new line
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
