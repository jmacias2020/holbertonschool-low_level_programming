#include <stdio.h>
/**
 * main - use put no allow use printf
 * Description: uses puts to print message
 * Return: 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
